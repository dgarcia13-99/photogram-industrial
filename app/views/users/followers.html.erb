<div class="row">
  <div class="col-md-6 offset-3">
  <h1>
    <%= @user.username %>
  </h1>

  <%= render "users/tabbed" %>
  
  </div>
</div>

<% @user.followers.each do |follower| %>
  <%= render "follow_requests/follow", person: follower %>
<% end %>
